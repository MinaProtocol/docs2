```ts
function checkZkappTransaction(transactionHash: string, blockLength: number): Promise<
  | {
  failureReason: string[][][];
  success: boolean;
 }
  | {
  failureReason: null;
  success: boolean;
}>
```

Defined in: [lib/mina/v1/fetch.ts:518](https://github.com/o1-labs/o1js/blob/46b9772d07785397c4bd76a102bc9f6730d321bb/src/lib/mina/v1/fetch.ts#L518)

## Parameters

### transactionHash

`string`

### blockLength

`number` = `20`

## Returns

`Promise`\<
  \| \{
  `failureReason`: `string`[][][];
  `success`: `boolean`;
 \}
  \| \{
  `failureReason`: `null`;
  `success`: `boolean`;
 \}\>
